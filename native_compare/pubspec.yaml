name: native_compare
description: Pull-Down button native compare

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  pull_down_button:
    path: ../

dev_dependencies:
  very_good_analysis: ^3.1.0